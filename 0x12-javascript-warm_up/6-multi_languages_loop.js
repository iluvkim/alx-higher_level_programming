#!/usr/bin/node

/*script to print strings using a loop */

let y;
const myStrings = ['C is fun', 'Python is cool', 'JavaScript is amazing'];

for (y = 0; y < 3; y++) {
  console.log(myStrings[y]);
}
